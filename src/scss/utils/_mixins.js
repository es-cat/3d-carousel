import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  // _mixins
  'hideText': {
    'display': 'block',
    'overflow': 'hidden',
    'textIndent': '110%',
    'whiteSpace': 'nowrap'
  }
});
